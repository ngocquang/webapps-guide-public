# webapps-guide-public

Run this script to deploy a web application with custom backend and frontend domains.

```bash
curl -fsSL https://raw.githubusercontent.com/ngocquang/webapps-guide-public/main/scripts/deploy_web.sh -o deploy_web.sh && chmod +x deploy_web.sh && ./deploy_web.sh && rm deploy_web.sh
```
